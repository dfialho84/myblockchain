describe("TransactionService", () => {
    describe("newTransaction", () => {
        it("should return a new transaction", () => {
            expect(false).toBeTruthy();
        });

        it("should throw an exception if the signature is invalid", () => {
            expect(false).toBeTruthy();
        });

        it("should throw an exception if the balance of the input is not equal the balcance of the wallet", () => {
            expect(false).toBeTruthy();
        });

        it("should throw an exception if the balance and the output sum are not equal", () => {
            expect(false).toBeTruthy();
        });
    });
});
